#include<bits/stdc++.h>
using namespace std;

int main()
{
    string num;
    cin>>num;
    int l = num.length();
    int ans = 0;
    for (int i = l - 1; i >= l-2; i--)
    {
        ans 
    }
        if (ans % 3 == 0)
            cout << "yes";
        else
            cout << "no";
    return 0;
}